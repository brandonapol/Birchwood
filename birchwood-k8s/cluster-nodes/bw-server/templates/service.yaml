apiVersion: v1
kind: Service
metadata:
  name: {{ include "bw-server.fullname" . }}
  labels:
    {{- include "bw-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bw-server.selectorLabels" . | nindent 4 }}
