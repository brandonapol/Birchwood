apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bw-cms.fullname" . }}-test-connection"
  labels:
    {{- include "bw-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bw-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
