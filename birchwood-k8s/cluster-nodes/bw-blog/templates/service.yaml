apiVersion: v1
kind: Service
metadata:
  name: {{ include "bw-blog.fullname" . }}
  labels:
    {{- include "bw-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bw-blog.selectorLabels" . | nindent 4 }}
