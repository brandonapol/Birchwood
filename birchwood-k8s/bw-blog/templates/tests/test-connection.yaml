apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bw-blog.fullname" . }}-test-connection"
  labels:
    {{- include "bw-blog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bw-blog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
