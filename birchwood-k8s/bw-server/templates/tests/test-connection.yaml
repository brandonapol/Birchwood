apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bw-server.fullname" . }}-test-connection"
  labels:
    {{- include "bw-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bw-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
